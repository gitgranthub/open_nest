# Open Nest application dependencies.
#
# Floors only for now. Phase 1 replaces these with exact pins once the versions have
# actually been exercised on Apple silicon (WORKORDER_01 section 35A: avoid installing
# the newest package versions on every launch).

PySide6>=6.7,<7
keyring>=25,<26
requests>=2.32,<3
